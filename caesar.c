#include <stdio.h>
#include <cs50.h>
#include <stdlib.h>
#include <string.h>
    //mainArg, mainChk

   // int key;

   // char cipher[];

int main(int argc, string argv[1])
{
    if(argc != 2 && argv[1] >= 0)
    {
        printf("Usage: ./caesar key");

    } else
    {
        string plainTxt = get_string("Plain text: ");
    }


    printf("%s", argv[1]);
}